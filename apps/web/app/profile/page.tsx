'use client';

import { ProfileScreen } from '@shipmobile/app';

/**
 * Profile page - renders the shared ProfileScreen component
 */
export default function ProfilePage() {
  return <ProfileScreen />;
}
